# Docker — Voir le README racine

Toute la procédure Docker (sans scripts shell d'init), la préparation du `.env`, la création idempotente des utilisateurs via `scripts/setup_users.py`, et les vérifications `scripts/verify_access.py` sont documentées dans le README principal, section:

- Exécution avec Docker (sans scripts shell d'init)

Utilisez `.env.example` à la racine comme modèle et copiez-le en `.env` avant de lancer `docker compose`.
